<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include layout="@layout/appbar_main" />

    <LinearLayout
        android:id="@+id/activityContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="?attr/actionBarSize"
        android:background="@android:color/black"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/viewBarcodeImage"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_margin="@dimen/standard_margin"
            android:layout_weight="0.7"
            android:scaleType="center"
            android:src="@drawable/sign_in_background"
            tools:ignore="ContentDescription" />

        <TextView
            android:id="@+id/viewBarcodeLabel"
            style="@style/TextAppearance.AppCompat.Title"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_margin="@dimen/standard_margin"
            android:layout_weight="0.3"
            android:lineSpacingMultiplier="1.1"
            android:textColor="@android:color/white"
            tools:text="0123456789012345678901234567890123456789012345678901234567891234" />

    </LinearLayout>

</android.support.design.widget.CoordinatorLayout>