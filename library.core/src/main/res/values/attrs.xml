<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Background Colors -->
    <attr name="colorBackground" format="color" />
    <attr name="circleBackgroundColor" format="color" />

    <!-- Button Styles -->
    <attr name="borderlessButtonColor" format="color" />

    <!-- Divider Colors -->
    <attr name="dividerColor" format="color" />

    <!-- Text Colors -->
    <attr name="textColorDisabled" format="color" />
    <attr name="textColorPrimaryOnly" format="color" />
    <attr name="textColorSecondaryOnly" format="color" />

    <!-- Theme Colors -->
    <attr name="colorPrimaryLight" format="color" />

</resources>